package com.campussphere.lostfound.entity;

/**
 * Whether a post reports an item the student lost, or an item the
 * student found and is holding for its owner to claim.
 */
public enum PostType {
    LOST,
    FOUND
}
